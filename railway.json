{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "playwright install chromium && gunicorn app:app --workers 2 --timeout 120",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
